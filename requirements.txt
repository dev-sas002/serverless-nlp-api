# Runtime dependencies for the container image and the Lambda package.
# spaCy is here; the *model* deliberately is not — that is the whole point of the
# project. See README "Why the model is not in the package".
flask>=3.0,<4
spacy>=3.7,<4
boto3>=1.34
requests>=2.31
python-dotenv>=1.0
gunicorn>=22.0
