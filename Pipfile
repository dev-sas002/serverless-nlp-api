[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
flask = ">=3.0,<4"
spacy = ">=3.7,<4"
boto3 = ">=1.34"
requests = ">=2.31"
python-dotenv = ">=1.0"
gunicorn = ">=22.0"

[dev-packages]
pytest = ">=8.0"
pytest-cov = ">=5.0"
moto = {extras = ["s3"], version = ">=5.0"}
ruff = ">=0.5"
zappa = "*"

[requires]
python_version = "3.12"
